import { tribes } from './tribes';
import { cardSets } from './cardSets';
import { cards } from './cards';

export { tribes, cardSets, cards };

// Relations will be defined here when needed
export const schema = {
  tribes,
  cardSets,
  cards,
};