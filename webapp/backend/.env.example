# Mythologia Backend - 環境変数テンプレート
# 
# 使い方:
# 1. ローカル開発: このファイルを .env.local にコピーして使用
# 2. Railway接続: このファイルを .env にコピーして使用（本番作業時のみ）

# === ローカル開発環境（Docker Compose）===
# 安全にローカルでテストするための設定
DATABASE_URL=postgresql://user:password@localhost:5432/mythologia_local
# Docker Compose使用時: postgresql://user:password@localhost:5432/mythologia_local

# Redis接続（ローカル）
REDIS_URL=redis://localhost:6379

# === Railway環境接続（本番作業時のみ）===
# ⚠️ 警告: 以下の行をコメントアウトして使用する場合は本番DBに接続されます
# DATABASE_URL=postgresql://postgres:xxx@xxx.proxy.rlwy.net:33024/railway

# Application Level Authentication（機密情報）
JWT_SECRET=your-jwt-secret-key-here
HMAC_SECRET=your-hmac-secret-key-here
ALLOWED_APP_IDS=mythologia-frontend

# Admin API Security（管理者API専用セキュリティ）
ADMIN_HMAC_SECRET=your-admin-hmac-secret-key-here
VERCEL_API_KEY=your-vercel-api-key-here

# CORS Settings（環境別設定）
CORS_ORIGINS=http://localhost:3000

# Production/Staging CORS Settings
# CORS_ORIGINS=https://your-production.vercel.app,https://your-staging.vercel.app

# 実行環境設定
NODE_ENV=development

# 管理者初期設定（初回セットアップ用）
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=secure-password-change-this
INITIAL_ADMIN_EMAIL=admin@example.com

# 将来のユーザー認証用（現在未使用）
# REFRESH_TOKEN_SECRET=your-refresh-token-secret

# 外部サービス（機密情報）
# SMTP_HOST=smtp.gmail.com
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Railway Production（自動設定される）
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# REDIS_URL=${{Redis.REDIS_URL}}
# PORT=${{PORT}}
# RAILWAY_PUBLIC_DOMAIN=https://your-service.up.railway.app


# 個人設定
# ENABLE_EXPERIMENTAL_FEATURES=true
# MY_CUSTOM_CONFIG=value